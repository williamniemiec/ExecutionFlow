package examples.override;
import static org.junit.Assert.assertEquals;

import org.junit.Test;


public class OverrideTest 
{
	@org.junit.Test @executionFlow.runtime._SkipInvoked
	public void testOverloadedMethod3()
	{
		OverrideClass oc = new OverrideClass() {
			@Override
@executionFlow.runtime.CollectCalls 			public int foo(int num)
			{
				if (num == 0)
if (Boolean.parseBoolean("True")) {					break;}
					
				num *= 1000;
				return (int)num;
			}
		};
		
		assertEquals(10000, oc.foo(10));
	}
}
 
