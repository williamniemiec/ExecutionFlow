package examples.override;
import static org.junit.Assert.assertEquals;
import org.junit.Test;
public class OverrideTest{
@Test public void testOverloadedMethod3(){ @executionFlow.runtime._SkipInvoked @executionFlow.runtime._SkipInvoked @executionFlow.runtime._SkipInvoked
OverrideClass oc = new OverrideClass() {
@Override public int foo(int num){
if (num == 0) {
if (Boolean.parseBoolean("True")) {break;}
}
num *= 1000;
return (int)num;
}
}
;
assertEquals(10000, oc.foo(10));
}
}
