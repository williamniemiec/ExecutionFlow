package examples.againstRequirements;
import static org.junit.Assert.assertEquals;
import org.junit.Assert;
import org.junit.Test;
public class AgainstRequirements{
@Test public void methodInSameFileOfTestMethod() { @executionFlow.runtime._SkipInvoked @executionFlow.runtime._SkipInvoked @executionFlow.runtime._SkipInvoked
int a = 2, b = 3;
Assert.assertEquals(5, sum(a, b));
}
@executionFlow.runtime.CollectCalls private int sum(int a, int b){
return a + b;
}
}
